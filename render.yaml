services:
  - type: web
    name: rsshub
    env: node
    plan: free
    buildCommand: pnpm install && pnpm build
    startCommand: pnpm start
    rootDir: .
    envVars:
      - key: NODE_ENV
        value: production

